telethon==1.34.0
google-generativeai==0.8.3
python-dotenv==1.0.0
colorama==0.4.6
asyncio
protobuf>=5.28.0

# Note: asyncio is built-in to Python 3.8+, no need to install separately
# protobuf 5.28.0+ is required for Python 3.14 compatibility
